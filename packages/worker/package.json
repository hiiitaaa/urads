{
  "name": "@urads/worker",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev --local --persist-to .wrangler/state --ip 0.0.0.0",
    "deploy": "wrangler deploy",
    "db:migrate": "wrangler d1 migrations apply urads-db --local"
  },
  "dependencies": {
    "hono": "^4.7.0",
    "@urads/shared": "workspace:*"
  },
  "devDependencies": {
    "wrangler": "^4.0.0",
    "@cloudflare/workers-types": "^4.20250320.0",
    "typescript": "^5.7.0"
  }
}
